# Candidate 5 HTTP rollout and capabilities: independent review

> Last updated: 2026-09-08 · commit `4ae34f033`

Reviewed all 47 text answers (45 unique texts), both vision answers against the supplied image, and the tool-call stream against its request. Independently reconstructed all 50 SSE responses and checked finish events, usage arithmetic, content/tool chunk counts, monotonic timestamps, first-content TTFT, one terminal DONE, empty reasoning, and absence of raw special-token markers. No stream-integrity discrepancy was found. Compact per-request checks and provenance are in `independent-evidence.json`.

All 47 text requests returned HTTP 200 and exactly twelve numbered, coherent implementation checks for the synthetic water-infrastructure task. Each checklist addresses plausible engineering planning or inspection actions; none asserts approved funding or completed project outcomes. Suggested equipment, monitoring, contingency and safety checks are proposals, not claimed source facts. All 47 naturally finish `stop`, at 169–223 reported completion tokens below the 256 cap. This provides observed natural-stop marker filtering, including the twelve requests begun after MTP activation; exact answer equivalence is not claimed.

Text TTFT was 22.2341s for the initial cold request. The next 46 ranged 171.916–184.214ms, with median 181.479ms. The twelve active-phase requests had median 175.860ms and maximum 183.190ms. Last inactive and first active metric observations bracket activation at monotonic 88.8335–89.0942s. Request 34 started inside that bracket and had TTFT 176.592ms; the neighboring requests were also within the normal range. There is no observed swap-related TTFT spike in this sequential workload. This does not certify arbitrary concurrent-load latency.

Both vision requests return the same coherent four-sentence description, naturally stopping after 107 reported completion tokens. The white/gray cube structure, black background, dark interface, side panels and visible controls match the independently viewed image. **Label caveat:** the answer calls a section “Camera Settings”; the actual visible labels are “Camera Position” and “Camera Target.” This is an imprecise control-label paraphrase, so the description is not claimed to be exact OCR. “Story Steps,” “Behavior,” “Story Builder,” “Visibility,” “Capture Camera,” “Load Matrix,” and “Export Story” are visible. Image bytes embedded in both requests exactly match the viewed file. Each vision request records 27 additional MTP rounds. TTFT is 1.53374s first and 0.45847s repeat.

The tool request returns exactly one structured `record_color` call with arguments `{"color":"blue","count":2}`, no prose or reasoning, and finish reason `tool_calls`. Its SSE arguments and function name reconstruct exactly; usage is 91 prompt+19 completion=110 total tokens. It records 6 MTP rounds and TTFT 203.173ms. This validates the emitted structured call; no external tool execution was requested or performed.

Vision/tool records report unchanged on-disk cache inventory and no reported cached-token count. That supports the narrower observed inventory/response claim; it does not prove zero RAM reuse or provide per-request SSD read measurements. Reported token usage arithmetic and chunk counts were checked; no independent tokenizer retokenization was performed, and completion-token counts need not equal SSE chunk counts.

Scope: one real M5 provider using default-auto settings with a controlled localhost catalog/CDN, pinned assistant bytes and synthetic text tasks. The delayed-download fixture omits the HF hint, so it does not validate public-catalog publication or HF-first runtime transport. It is neither a physical fleet test nor a broad semantic-quality/equivalence evaluation. Other evidence, including the generated code defects in separate greedy runs, remains separate and is not overridden by this successful HTTP check.
